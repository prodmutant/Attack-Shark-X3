__pycache__/
*.pyc

# python build products
dist/
build/
*.egg-info/

# driver build products. The .sys is signed with a certificate generated on
# the machine that built it, which is trusted nowhere else - so it is of no
# use to anyone downloading it. Build your own: python tools/build_driver.py
driver/build/
driver/out/

# your own logo/backdrop, kept out of the repo
attackshark/web/*.custom.*

# Screenshots the RE tooling takes while driving the vendor UI. Working
# material: 14 MB of it, nothing reads it back, and it was most of the
# repository's weight. tools/exercise.py still writes here.
captures/shots/
